# Wolf3D shareware (obtain separately)
shareware/

# Local reference dumps / reverse-engineering scratch
refs/

# All build/generator output (tools/gen_*.py, pack_*.py, extract_*.py, ACME,
# c1541) lives here — rebuild with build.bat. wolf64.d64/wolf64.crt are the
# exception: kept at repo root and tracked, for clone-and-play.
generated/

# Third-party tools, not redistributed
3rdparty/
__pycache__/
*.pyc

# Editor / OS
.DS_Store
Thumbs.db
*.swp
.vscode/
.idea/
